---
- name: Install node_exporter from local repo
  ansible.builtin.package:
    name: "{{ node_exporter_package }}"
    state: present

- name: Enable and start node_exporter
  ansible.builtin.systemd:
    name: node_exporter
    state: started
    enabled: true
